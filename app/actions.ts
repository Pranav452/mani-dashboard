'use server'

// Phase 1 stub: this frontend is currently a pure shell.
// We intentionally do NOT connect to Supabase or any real backend here.
// Later, you will replace this implementation to call your real API.
export async function getShipments() {
  console.warn("getShipments() is in stub mode for the MVP. Returning an empty dataset until a real API is wired up.")
  return []
}